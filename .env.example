# Cloud backend (optional). Without these, the app runs fully offline/local-only.
# Copy to `.env` and fill in from your Supabase project settings.
# See BACKEND.md for the schema (supabase/schema.sql) these connect to.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
